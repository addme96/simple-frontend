export default function About() {
    return (
        <p>
            This is simple frontend app that demonstrates CRUD operations on a REST resource called
            "Resource".
            Its only properties are ID and Name.
            Simple frontend is part of Dawid Dudek's Master's thesis.
        </p>
    );
}
